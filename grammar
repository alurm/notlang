whitespace = "\t" | " "

separator = "\n" | ";"

letter = /* Everything else */

word = letter { letter }

statement = word { whitespace word }

statements = statement [ separator statements ]

function_call = "[" statements "]"

function_definition = "{" statements "}"

grammar = statements